//Manipulates the users stuff
const router = require('express').Router()
//Psuedo code
const database = null;

//Routes
router.get("/", () => null())
router.patch("/", () => null())
router.put("/", () => null())

module.exports = router
