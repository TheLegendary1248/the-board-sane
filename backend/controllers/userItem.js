//Manipulates the users stuff
const router = require('express').Router()
//Psuedo code
const database = null;

//TODO Note Last-Modified HTTP header for respecting cached data


//Routes
router.get("/", () => null())
router.patch("/", () => null())
router.put("/", () => null())

module.exports = router
